import { DragDropFile } from './drag-drop-file';

describe('DragDropFile', () => {
  it('should create an instance', () => {
    const directive = new DragDropFile();
    expect(directive).toBeTruthy();
  });
});
